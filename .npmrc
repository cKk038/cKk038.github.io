# 本项目固定使用 npm 官方源，不要删掉这一行。
#
# 原因：GitHub Actions 的构建机在美国，拉国内的 registry.npmmirror.com
# （淘宝镜像）会失败，而国内本地拉它毫无问题 —— 于是就会出现
# 「本地 npm run build 一切正常，推上去 CI 的 npm ci 秒退」这种极难排查的问题。
#
# 把源固定成官方源后，lockfile 里的下载地址也是官方地址，CI 拉的是自己的源，稳定。
# 想在国内临时用镜像加速，可以命令上加参数覆盖：
#     npm install --registry=https://registry.npmmirror.com
# 但不要用 `npm install` 之后把改动过的 package-lock.json 提交上去，
# 那会把下载地址又写回镜像。提交前跑 `node _tools/check-deploy.mjs` 会拦住这种情况。
registry=https://registry.npmjs.org/